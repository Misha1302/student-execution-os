<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds may reach a development server on the LAN over plain HTTP
     (e.g. http://192.168.1.10:8765). Release builds use src/main (HTTPS only). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
